\documentclass[tikz, border=15pt]{standalone}

\usepackage{fontawesome5}
\usepackage{xcolor}
\usetikzlibrary{positioning, fit, backgrounds, shadows, shapes.geometric, arrows.meta}

\definecolor{hwfill}{HTML}{E8F5E9}
\definecolor{hwdraw}{HTML}{2E7D32}
\definecolor{cloudfill}{HTML}{E3F2FD}
\definecolor{clouddraw}{HTML}{1565C0}
\definecolor{dbfill}{HTML}{FFE0B2}
\definecolor{dbdraw}{HTML}{E65100}
\definecolor{uifill}{HTML}{EDE7F6}
\definecolor{uidraw}{HTML}{4527A0}
\definecolor{groupfill}{HTML}{9E9E9E}

\tikzset{
    hw/.style={
        rectangle, rounded corners=4pt,
        draw=hwdraw, very thick,
        fill=hwfill,
        drop shadow,
        text centered,
        font=\small\sffamily,
        minimum height=1cm,
        minimum width=2.6cm,
        inner sep=6pt
    },
    cloud/.style={
        rectangle, rounded corners=4pt,
        draw=clouddraw, very thick,
        fill=cloudfill,
        drop shadow,
        text centered,
        font=\small\sffamily,
        minimum height=1.2cm,
        minimum width=3cm,
        inner sep=6pt
    },
    db/.style={
        cylinder, shape border rotate=90, aspect=0.3,
        draw=dbdraw, very thick,
        fill=dbfill,
        drop shadow,
        text centered,
        font=\small\sffamily,
        minimum height=1.6cm,
        minimum width=2.8cm,
        inner sep=4pt
    },
    ui/.style={
        rectangle, rounded corners=4pt,
        draw=uidraw, very thick,
        fill=uifill,
        drop shadow,
        text centered,
        font=\small\sffamily,
        minimum height=1cm,
        minimum width=2.8cm,
        inner sep=6pt
    },
    group/.style={
        rectangle, rounded corners=8pt,
        draw=black!60, thick, dashed,
        fill=groupfill, fill opacity=0.08,
        inner sep=14pt
    },
    grouptitle/.style={
        font=\small\bfseries\sffamily,
        text=black!75
    },
    arrow/.style={
        thick, ->, >=Stealth,
        font=\footnotesize\sffamily
    },
    arrowbi/.style={
        thick, <->, >=Stealth,
        font=\footnotesize\sffamily
    }
}

\begin{document}
\begin{tikzpicture}[node distance=1cm and 1.6cm]

    % =================== ZONA 1: EDGE AI ===================
    \node[hw] (sen) {\faTint~Sensor SEN0193 \\ \scriptsize (Humedad suelo)};
    \node[hw, below=of sen] (dht) {\faThermometerHalf~Sensor DHT22 \\ \scriptsize (Temp / Humedad aire)};
    \node[hw, below=of dht] (rtc) {\faClock~Función millis() \\ \scriptsize (Referencia temporal)};

    \node[hw, right=2.2cm of dht, minimum width=4cm, minimum height=2.6cm,
          align=center] (esp32)
        {\faMicrochip~\textbf{ESP32} \\ Microcontrolador \\[3pt]
         \scriptsize \fbox{\texttt{modelo\_edge.h}} \\
         \scriptsize TinyML · Decision Tree};

    \node[hw, below=1.4cm of esp32, fill=hwfill!70] (valve)
        {\faTint*~Electroválvula \\ \scriptsize (Actuador físico)};

    % Flechas sensores -> ESP32
    \draw[arrow] (sen.east) -- (esp32.west |- sen.east);
    \draw[arrow] (dht.east) -- (esp32.west);
    \draw[arrow] (rtc.east) -- (esp32.west |- rtc.east);

    % ESP32 -> Valvula
    \draw[arrow] (esp32.south) -- node[right, font=\scriptsize\sffamily, align=left]
        {Inferencia \\ Local Offline} (valve.north);

    % =================== ZONA 2: CLOUD ===================
    \node[cloud, right=3.2cm of esp32, minimum height=2cm] (flask)
        {\faServer~\textbf{Flask REST API} \\ \scriptsize Orquestador};

    % ESP32 -> Flask
    \draw[arrow] (esp32.east) -- node[above, align=center, font=\scriptsize\sffamily]
        {POST /telemetry \\ (Si hay WiFi)} (flask.west);

    % =================== ZONA 3: ALMACENAMIENTO ===================
    \node[db, above=1.6cm of flask] (firebase)
        {\faDatabase~\textbf{Firebase} \\ \scriptsize Realtime DB};

    \draw[arrowbi] (flask.north) -- node[right, font=\scriptsize\sffamily] {Sync} (firebase.south);

    % =================== ZONA 4: INTERFACES ===================
    \node[ui, right=2.8cm of firebase] (dash)
        {\faDesktop~\textbf{Dashboard} \\ \scriptsize Streamlit};

    \node[ui, right=2.8cm of flask] (telegram)
        {\faMobileAlt~\textbf{Bot de Telegram}};

    % Firebase -> Dashboard
    \draw[arrow] (firebase.east) -- node[above, font=\scriptsize\sffamily] {Lectura} (dash.west);

    % Dashboard -> Flask (override)
    \draw[arrow] (dash.south) -- ++(0,-0.5)
        -| node[pos=0.25, above, font=\scriptsize\sffamily] {POST /override} (flask.north east);

    % Flask <-> Telegram
    \draw[arrow] (flask.east) -- ++(0.4,0.2) -|
        node[pos=0.25, above, font=\scriptsize\sffamily] {Notificaciones}
        (telegram.north);
    \draw[arrow] (telegram.south) |- ++(-0.4,-0.4) -|
        node[pos=0.75, below, font=\scriptsize\sffamily] {POST /webhook}
        (flask.south east);

    % =================== GRUPOS (backgrounds) ===================
    \begin{scope}[on background layer]
        \node[group, fit=(sen)(dht)(rtc)(esp32)(valve),
              label={[grouptitle, anchor=north]above:Capa Edge AI / Entorno de Parque}]
              (g1) {};

        \node[group, fit=(flask)(firebase),
              label={[grouptitle, anchor=north]above:Capa Cloud / Orquestación}]
              (g2) {};

        \node[group, fit=(dash)(telegram),
              label={[grouptitle, anchor=north]above:Capa de Control e Interfaz}]
              (g3) {};
    \end{scope}

\end{tikzpicture}
\end{document}